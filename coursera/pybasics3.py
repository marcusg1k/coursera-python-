#define function greeting with parameter name
def greeting(name):
    print("Welcome, " + name)
    #print welcome + parameter name

greeting("Marcus")
#call function greeting with argument name (actually input the name)